function printReverse(firstChar, secondChar, thirdChar) {
    let result = thirdChar + ' ' + secondChar + ' ' + firstChar;
    console.log(result);
}

// example input:
printReverse('A', 'B', 'C');
printReverse('1', 'L', '&');